# BETTER — what this repo is

**mcp-better — built for 7/28**  
Textbook for **AGENTS.md** on **NONE | GOOD | [BETTER] | BEST**.  
*(protocol **2026-07-28** — the modern MCP release)*

```text
the book is the app is the book
```

This repo is a **textbook that runs** — not a server with optional docs.  
It is about **AGENTS.md**-grade agent context discipline on the MCP wire: honest **BETTER**, not BEST product DNA.  
Lessons live in [`textbook/`](./textbook/). The binary proves them.  
Doctrine: [`textbook/DOCTRINE-book-is-app.md`](./textbook/DOCTRINE-book-is-app.md).

| Name | What it is |
|------|------------|
| **7/28** | Era name — speakable, brandable |
| **2026-07-28** | Protocol version — date string on the wire |

```
NONE  →  GOOD  →  BETTER  →  BEST
                      ▲
                   this repo
                 (AGENTS.md · MCP)
```

## Ladder

| Tier | Meaning |
|------|---------|
| **NONE** | No MCP / no agent context discipline |
| **GOOD** | MCP works (often legacy initialize, unstamped lists, mixed protocol claims) |
| **BETTER** | Modern MCP **honestly** built for **7/28** — Discover, stamped lists, claimed = tested · **AGENTS.md**-ready protocol surface |
| **BEST** | Persistent project DNA for agents (**AGENTS.md** / FAF) — **[faf.one/agents](https://faf.one/agents)** |

## mcp-better = BETTER textbook for AGENTS.md (7/28)

| Face | Location |
|------|----------|
| **Book** | [`textbook/`](./textbook/) — what · why · how |
| **App** | `src/` · `examples/` · CI — wire proof |
| **Key** | **AGENTS.md** ladder — this repo sits on **[BETTER]** |
| **Identity** | Claim surface ≤ proof surface · same 7/28 era across versions |

### Lesson releases (versions compound)

| Version | Lesson |
|---------|--------|
| **v0.1** | 7/28 over **stdio** — Discover path, stamped list, stable order, `health` + `echo` |
| **v0.2** | Same era + **Streamable HTTP** road (`--http`) + routing-header smoke |
| **v0.3** | Same era + **deeper correctness** — multi-list + restart-order smokes · `mcp-worse` contrast |
| **v0.4** | Same era + **dual package** — cargo + npm shim |
| **v0.4.3** | Same era + **`confirm_echo` MRTR** + Agent Skills (`mcp-better-lab`) |
| **v0.4.4** | Same era + **book matches 0.4.3 wire** — no new tool |
| Later | More road / louder proof inside 7/28 — never a new “protocol era” by semver alone |

Also: official **`rmcp` 3.0** · Registry `server.json` · **no** `project.faf` required on `main`.

## Not BEST

BEST is a different product surface (persistent AI context). One line:

→ https://faf.one/agents
