/target/
**/*.rs.bk
.DS_Store
.env
.env.*
!.env.example
# project.faf intentionally NOT ignored on better-best/* branches — that's
# the BEST print slice itself. Ignored again the moment this merges to main.
.faf
CLAUDE.md
AGENTS.md

# mcp-publisher local auth (never commit)
.mcpregistry_github_token
.mcpregistry_registry_token
.mcp_publisher_token

# npm shim
node_modules/
package-lock.json
# binary cache for local npm shim tests
.cache/
