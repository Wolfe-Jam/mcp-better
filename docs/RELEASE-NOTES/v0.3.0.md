# mcp-better v0.3.0 — release note

**Lesson:** deeper correctness — harder to lie about the list contract.  
**Era:** **7/28** · wire **`2026-07-28`** (more road, not a new protocol era)  
**Ship SHA:** `8a74697`

```text
the book is the app is the book
```

## Claims this version

| Surface | Status |
|---------|--------|
| Discover + stamped `tools/list` (`ttlMs=60000`, `cacheScope=public`) | proved |
| Tools on **mcp-better** = `health` → `echo` only | proved |
| Multi-list stability (stdio-client ×6) | proved |
| Restart-stable order (`order-restart-smoke`, two processes) | proved |
| Streamable HTTP local demo + `Mcp-Method` / `Mcp-Name` | proved |
| Lying companion **`mcp-worse`** + `contrast-smoke` | proved (teaching only) |
| Season 1 textbook ships in-tree (`textbook/`) | shipped |

## Proof

- **crates.io:** [mcp-better 0.3.0](https://crates.io/crates/mcp-better/0.3.0)
- **GitHub:** tag `v0.3.0` · commit `8a74697`
- **CI:** green on ship SHA
- **Lab:** [textbook/09-run-the-textbook.md](https://github.com/Wolfe-Jam/mcp-better/blob/v0.3.0/textbook/09-run-the-textbook.md)
- **Doctrine:** [textbook/DOCTRINE-book-is-app.md](https://github.com/Wolfe-Jam/mcp-better/blob/v0.3.0/textbook/DOCTRINE-book-is-app.md)

## Not claimed

- Production remote HTTP (auth / TLS / open bind)
- OAuth, resources, prompts, tasks as product surface
- That **`mcp-worse`** is a supported peer (it is intentionally dishonest)
- A new protocol “era” — still **7/28**

## Install

```bash
cargo install mcp-better --version 0.3.0
mcp-better --help

# teaching only — do not point hosts at this:
# cargo install mcp-better --version 0.3.0 --bin mcp-worse
```

From source:

```bash
git clone https://github.com/Wolfe-Jam/mcp-better.git
cd mcp-better
cargo build --bins
cargo run --example stdio-client
cargo run --example order-restart-smoke
MCP_WORSE_BIN=target/debug/mcp-worse cargo run --example contrast-smoke
```

## Changelog (summary)

- Louder smokes: multi-list, process restart order, better vs worse contrast
- `mcp-worse` binary for curriculum
- Textbook Season 1 + book=app doctrine

Full detail: [CHANGELOG.md](https://github.com/Wolfe-Jam/mcp-better/blob/v0.3.0/CHANGELOG.md)

---

*BETTER textbook MCP — claim surface ≤ proof surface.*
